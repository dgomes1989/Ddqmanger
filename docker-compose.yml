# Optional: Only needed for local development without Supabase.
# The primary setup uses Supabase for both database and file storage.
# If you want to develop locally with Docker instead, uncomment below.

# services:
#   db:
#     image: pgvector/pgvector:pg16
#     environment:
#       POSTGRES_USER: ddqmanager
#       POSTGRES_PASSWORD: ddqmanager
#       POSTGRES_DB: ddqmanager
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#
# volumes:
#   pgdata:
